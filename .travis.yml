env:
   global:
     - CONAN_REFERENCE: "Grawlog/0.1"
     - CONAN_USERNAME: "strootje"
     - CONAN_LOGIN_USERNAME: "strootje"
     - CONAN_CHANNEL: "conan"
     - CONAN_UPLOAD: "https://api.bintray.com/conan/strootje/conan"
     - CONAN_REMOTES: "https://api.bintray.com/conan/conan-community/conan"

linux: &linux
   os: linux
   sudo: required
   language: python
   python: "3.6"
   services:
     - docker

osx: &osx
   os: osx
   language: generic

matrix:
   include:
      # - <<: *linux
      #   env: CONAN_CLANG_VERSIONS=3.9 CXXFLAGS="-std=c++17" CONAN_DOCKER_IMAGE=lasote/conanclang39

      - <<: *linux
        env: CONAN_CLANG_VERSIONS=4.0 CXXFLAGS="-std=c++17" CONAN_DOCKER_IMAGE=lasote/conanclang40

install:
  - chmod +x .travis/install.sh
  - ./.travis/install.sh

script:
  - chmod +x .travis/run.sh
  - ./.travis/run.sh
